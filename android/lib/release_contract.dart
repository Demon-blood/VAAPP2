/// Stable Android/backend compatibility contract for the Full-Time VA v1 line.
///
/// Keep build metadata in pubspec.yaml. Runtime compatibility checks use these
/// semantic versions so setup, repair and diagnostics cannot drift apart.
const String appRelease = '1.0.6';
const String minimumBackendVersion = '1.0.6';
