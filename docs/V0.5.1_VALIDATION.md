# v0.5.1 validation record

Packaging-time checks completed:

- `python -m compileall -q backend/app backend/tests` — passed.
- `pytest -q backend/tests/test_version_and_routes.py` — 2 passed.
- release markers verified: backend 0.5.1, Android 0.5.1+23, GitHub Actions v0.5.1 APK filename.
- new `FinancialRecord` model, financial-document policy, reconciliation service and `/api/financial-records` routes verified present.
- edited Dart files passed delimiter/structure checks.

The packaging environment does not contain `aiosqlite`, Ruff, Flutter or Dart. Therefore the database-backed pytest suite, Ruff, `flutter analyze`, `flutter test`, and signed release APK build were not claimed as locally passed. The included GitHub Actions workflow installs the backend dependencies (including `aiosqlite`) and remains the authoritative full regression/build gate.
