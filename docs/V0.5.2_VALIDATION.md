# v0.5.2 validation record

Validated in the packaging environment on 2026-08-10.

## Passed locally

- `python3 -m compileall -q backend/app backend/tests`
- `PYTHONPATH=backend pytest -q backend/tests/test_version_and_routes.py` -> 2 passed
- backend release marker: `APP_VERSION = "0.5.2"`
- Android release marker: `version: 0.5.2+24`
- GitHub Actions artifact name: `Full-Time-VA-Android-v0.5.2.apk`
- Android edited-file delimiter balance check
- source archive integrity check before delivery

## Added regression coverage

The source includes tests for:

- Gmail HTTP 409 operation-boundary retry;
- non-transient HTTP 400 not being retried;
- label-create race recovery by re-listing and adopting the competing worker's label;
- dead-letter failure-signature compaction;
- bulk Autopilot exception recovery;
- one-time repair of the pre-v0.5.2 Gmail 409 retry/dead-letter backlog.

## Local environment limitations

The packaging environment does not have the backend's complete dependency set
(`google-api-python-client`, SQLAlchemy/aiosqlite in the active Python environment),
nor Flutter/Dart/Ruff. Therefore the new dependency-backed pytest files, Ruff,
`flutter analyze`, `flutter test`, and signed APK build were not claimed as passed locally.

The included GitHub Actions workflow installs `backend[dev]` and remains the
authoritative complete regression and release gate.
