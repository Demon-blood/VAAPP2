# v0.6.2 validation record

This source tree is v0.6.1 Autopilot Completion plus the Gmail conflict hardening required by the live Render logs from 2026-08-10.

## Version contract

- Backend: `APP_VERSION = "0.6.2"`
- Backend package: `0.6.2`
- Android: `0.6.2+27`
- Release APK: `Full-Time-VA-Android-v0.6.2.apk`

## Local validation performed

- Python compileall over `backend/app` and `backend/tests`.
- YAML parsing of the Android release workflow.
- `bash -n` validation of each shell `run:` block in the release workflow.
- Source-level structural checks for version, Gmail conflict-recovery wiring, one-time backlog retirement, and timezone-aware scheduler startup.
- `pytest -q backend/tests/test_daily_briefing_integration.py backend/tests/test_v062_autopilot_contract.py` — **10 passed**.

The targeted pytest module cannot execute in this sandbox because `googleapiclient` is not installed here. The GitHub release workflow installs the backend dependency set and executes the complete backend test suite before publishing the APK.
